# 0x06 Unittests in JS

## Description

This project demonstrates basic unit testing in JavaScript using Mocha and Node's `assert` module.

## Files

- **0-calcul.js**: Function that rounds two numbers and returns their sum.
- **0-calcul.test.js**: Unit tests for `0-calcul.js`.

## Usage

1. Install dependencies:
   ```bash
   npm install
2.Run tests:
	npm test 0-calcul.test.js


### Step 3: Commit and Push

```bash
git add .
git commit -m "Add calculateNumber function and tests"
git push

